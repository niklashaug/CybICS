#!/bin/sh /etc/rc.common

START=95
STOP=15
USE_PROCD=1

start_service() {
    procd_open_instance
    procd_set_param command /usr/sbin/dropbear -F -E -R -p 22 -p 2222
    procd_set_param respawn 5 5 0
    procd_close_instance
}

